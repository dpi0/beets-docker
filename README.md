# Beets - Docker

This docker image is `lscr.io/linuxserver/beets` + some plugins.

For the list of added plugins, refer `./requirements.txt`

Each image is tagged with the hash of the latest build commit, for example: ghcr.io/dpi0/beets-docker:01e8aee
